server:
  port: 9999

management:
  security:
    enabled: false

  endpoints:
    web:
      exposure:
        include: refresh

eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false